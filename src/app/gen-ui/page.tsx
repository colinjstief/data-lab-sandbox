import { NextPageParams, NextPageSearchParams } from "@/lib/types";

const GenUIPage = ({
  params,
  searchParams,
}: {
  params: NextPageParams;
  searchParams: NextPageSearchParams;
}) => {
  return <p>Generative UI coming soon</p>;
};

export default GenUIPage;
